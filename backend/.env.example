# Sample_data location (resolved as Path).
# Default is <repo>/../sample_data (sibling of omix_tpd).
# Override if your data is elsewhere.
# OMIXAI_DATA_ROOT=/mnt/c/Users/beloz/Documents/sample_data

# Drug info crawler cache file.
# OMIXAI_DRUG_INFO_CACHE=./var/drug_info_cache.json

# Allowed CORS origins (comma-separated)
# OMIXAI_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
